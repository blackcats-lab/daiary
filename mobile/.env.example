# Flutter から読み込まれる環境変数（flutter_dotenv 経由）
# 本ファイルをコピーして mobile/.env を作成し、実値を入れる。
# .env は .gitignore 対象。

# 環境識別 (dev | staging | prod)
ENV=dev

# Supabase 接続情報（`supabase status` の出力 or Supabase Dashboard で確認）
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
